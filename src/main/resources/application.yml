logging:
  level.: ERROR
  level.org.springframework: ERROR
  level.org.springframework.web: ERROR
  level.org.hobsoft.spring.resttemplatelogger.LoggingCustomizer: DEBUG
server:
  servlet:
    context-path: "/begrep/"
management:
  server:
    port: 8181
  endpoints:
    web:
      base-path: "/begrep/"
      exposure:
        include: "prometheus,metrics,info,health"
